{% set name = "pytest-asyncio-cooperative" %}
{% set version = "0.32.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 62f6186188162b5ddefaeb42bf93e9c70fcf58de891b03eadd80445c4f7c2eab

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
    - poetry-core >=1.0.0
  run:
    - python >=3.7
    - pytest

test:
  imports:
    - pytest_asyncio_cooperative
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/pytest-asyncio-cooperative/
  summary: Run all your asynchronous tests cooperatively.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - MichelML
